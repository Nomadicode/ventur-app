mutation RemoveFriendFromGroup($groupId: Int!, $memberId:Int!){
  removeFriendFromGroup(groupId: $groupId, memberId: $memberId) {
    success
    error
    group {
      pk
      id
      name
    }
  }
}